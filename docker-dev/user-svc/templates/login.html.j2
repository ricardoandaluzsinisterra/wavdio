<!DOCTYPE html>
<html>
<head>
    <title>Login Page</title>
    <link rel="stylesheet" type="text/css" href="{{ url_for('static', filename='styles.css') }}">
</head>
<body>
    <div class="login-container">
        <img src="{{ url_for('static', filename='image/pp.jpg') }}" alt="Logo">
        <h2>Login</h2>
        <form action="/login" method="post">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <input type="submit" value="Login">
        </form>
        {% if error %}
            <p style="font-size: 0.8em; color:red;" class="error">{{ error }}</p>
        {% endif %}
        <p style="text-align: right; font-size: 0.8em;">Don't have an account yet? <a href="/register" style="color: blue;">Register here.</a></p>
    </div>
</body>
</html>